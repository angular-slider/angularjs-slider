$scope.slider = {
  minValue: 20,
  maxValue: 80,
  options: {
    floor: 0,
    ceil: 100,
    step: 10,
    disabled: true,
    showTicks: true, // just to show the disabled style
    draggableRange: true // just to show the disabled style
  }
};
